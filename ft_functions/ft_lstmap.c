#include "libft.h"

t_list	*ft_lstmap(t_list *lst, void *(*f)(void *), void (*del)(void *));

t_list	*ft_lstmap(t_list *lst, void *(*f)(void *), void (*del)(void *))
{
	if (!lst)
		return (NULL);
	t_list *copy;
	t_list *cursor;

	copy = ft_lstnew()
	while (lst != NULL)
	{
		copy = malloc (sizeof(t_list *));
		copy-content
	}
}
